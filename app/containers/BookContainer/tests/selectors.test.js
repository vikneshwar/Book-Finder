// import { selectBookContainerDomain } from '../selectors';

describe('selectBookContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
